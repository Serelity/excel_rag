channels:
  - conda-forge
dependencies:
  - python=3.11
  - openjdk=21
  - pip>=24,<26
  - setuptools>=75
  - wheel>=0.44

# The companion creation script installs the locked Python packages. Java 21
# is isolated here because Pyserini 1.2.0 uses Lucene/Anserini through PyJNIus.
